/**
 * Simple scroll to top hook stub
 * Returns boolean for scroll visibility
 */
const useScrollToTop = (threshold = 300) => {
  return false; // Not visible for now
};

export { useScrollToTop };
export default useScrollToTop;